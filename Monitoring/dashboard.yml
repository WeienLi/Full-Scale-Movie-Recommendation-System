apiVersion: 1

providers:
  - name: "Default"
    folder: "Team 3"
    folderUid: ""
    type: file
    allowUiUpdates: false
    updateIntervalSeconds: 60
    options:
      path: /var/lib/grafana/dashboards
